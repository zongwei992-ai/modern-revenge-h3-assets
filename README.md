# MiniMax H3 reference assets

Public, AI-generated visual reference assets used by the MiniMax H3 video
workflow for the short-drama project `modern-revenge-ep01`.

The directory structure is intentionally stable because the production
manifest resolves assets through GitHub Raw URLs.

This repository contains only generated reference images. It does not contain
API credentials, local configuration, scripts, story documents, or rendered
videos.
